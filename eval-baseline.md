# 测试集与 Baseline 评测

## 测试集说明

- **文件**: `data/math_test.jsonl`
- **题目数**: 41 道
- **格式**: 与训练数据一致，`id`, `question`, `answer`, `num_terms`, `num_digits`
- **难度分布**:

| 维度 | 范围 | 题数 |
|---|---|---|
| 运算类型 | 加法 | 14 |
| | 减法 | 15 |
| | 乘法 | 12 |
| | 三项运算 | 9 |
| 位数 | 1 位 | 3 |
| | 2 位 | 27 |
| | 3 位 | 11 |

- **答案验证**: 已通过 `verify_test_answers.py` 逐题用 Python `eval()` 验算，全部 41 道题答案正确。

## 评测脚本

**文件**: `eval.py`

```
用法:
    python eval.py                              # 评测基座模型
    python eval.py --checkpoint output/step_20  # 评测训练后的 checkpoint
    python eval.py --num_samples 8              # 每题采样 8 次 (默认 4)
    python eval.py -v                           # 打印每题详细结果
```

**评测指标**:

| 指标 | 含义 |
|---|---|
| exact@1 | `<answer>` 标签内答案与标准答案完全一致 |
| pass@1 | 标准答案出现在 `<answer>` 标签内（包含关系） |
| 无答案率 | 未生成 `<answer>` 标签的样本比例 |
| `<think>` 标签率 | 生成 `<think>` 开头标签的比例 |
| `</think>` 标签率 | 生成 `</think>` 闭合标签的比例 |
| `<think>` 成对率 | `<think>` 和 `</think>` 都出现的比例 |
| `<answer>` 成对率 | `<answer>` 和 `</answer>` 都出现的比例 |

**评测流程**:
1. 加载 4-bit Qwen2.5-0.5B-Instruct（如有 checkpoint 则加载 LoRA adapter 并 merge）
2. 对每道题用固定的 DeepSeek 风格 system prompt 生成 `num_samples` 次
3. 用正则 `r"<answer>(.*?)</answer>"` 提取答案
4. 检查 `<think>` 和 `<answer>` 标签完整性
5. 统计 exact match / partial match / no answer / tag format

## Baseline 结果

**模型**: `Qwen/Qwen2.5-0.5B-Instruct` (4-bit, 未经训练)
**配置**: temperature=1.0, top_p=1.0, max_length=512, num_samples=8
**评测时间**: 2026-05-31

```
总题目数: 41
每题采样: 8 次
总推理次数: 328

============================================================
正确率指标
============================================================
精确匹配率 (exact@1):     11/328 = 3.4%
包含答案率 (pass@1):      18/328 = 5.5%
未生成 <answer> 标签:     299/328 = 91.2%

============================================================
标签格式统计
============================================================
<think> 标签:             39/328 = 11.9%
</think> 标签:            30/328 = 9.1%
<think>...</think> 成对:  30/328 = 9.1%
<answer> 标签:            30/328 = 9.1%
</answer> 标签:           31/328 = 9.5%
<answer>...</answer> 成对: 29/328 = 8.8%
```

### 分析

1. **91.2% 的样本没有输出 `<answer>` 标签** — 基座 Qwen 模型没用过 DeepSeek 的 think/answer 格式，大部分回答只是纯文本叙述，如 "The sum of 321 and 654 is 975."

2. **即使模型算对了也拿不到分** — 很多样本输出了正确数值（如 `700 - 263 =` 几乎所有样本都答了 437），但因为没包在 `<answer>` 标签里，reward 为 0。

3. **标签格式掌握很差** — `<think>` 成对率仅 9.1%，`<answer>` 成对率仅 8.8%。基座模型偶尔会输出标签（可能是训练数据中见过类似格式），但远未形成稳定行为。

4. **3.4% 的 exact match 来自偶发行为** — 少数样本碰巧输出了 `<answer>` 标签且答案正确，如 15+27=42（简单加法）和 18×5=90（常见乘法）。

5. **简单题表现更好** — 1 位数的乘法和熟悉的运算 exact match 率更高，说明模型对常见运算有记忆，但缺乏系统性的推理能力。

通过 GRPO 训练后，预期：
- `<answer>` 标签率大幅提升（模型学会格式化答案）
- `<think>` 标签率提升（模型学会展示推理过程）
- exact@1 逐步提高（数学正确率随奖励信号上升）
- pass@1 与 exact@1 的差距缩小（答案更精确而非只包含关系）
